# Testing Archive

This folder contains historical ad hoc test scripts and exploratory prototypes. It is not part of the active workflow.

Use `pipeline.yml` for runnable pipeline order and `tests/` for current smoke tests.
